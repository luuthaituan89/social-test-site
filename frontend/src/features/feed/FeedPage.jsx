import{assetUrl}from"../../services/api";
export function FeedPage({composer,posts,empty,suggestedGroups=[],onOpenGroup,footer}){return <section className="feed-page" data-module="feed">{composer}{posts.length?posts:empty}{suggestedGroups.length>0&&<aside className="card feed-group-suggestions"><header><div><h2>Groups you may like</h2><small>Public communities suggested for you</small></div></header><div>{suggestedGroups.map(group=><button key={group.id} onClick={()=>onOpenGroup?.(group.id)}>{group.cover_url?<img src={assetUrl(group.cover_url)} alt=""/>:<span>👥</span>}<i><b>{group.name}</b><small>{group.member_count} members</small></i></button>)}</div></aside>}{footer}</section>}
